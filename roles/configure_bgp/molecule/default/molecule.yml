---
dependency:
  name: galaxy

driver:
  name: delegated

platforms:
  - name: external-lab

provisioner:
  name: ansible
  inventory:
    links:
      hosts: ../tests/inventory.example.ini
  playbooks:
    converge: converge.yml

verifier:
  name: ansible
  playbooks:
    verify: verify.yml
