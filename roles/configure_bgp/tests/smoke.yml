# purpose: Smoke test for role configure_bgp
# maintainer: HybridOps.Studio

- name: Smoke test for role configure_bgp
  hosts: configure_bgp_test
  gather_facts: false
  become: false

  collections:
    - hybridops.network

  roles:
    - role: hybridops.network.configure_bgp
